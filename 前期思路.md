搭建一个基于Next.js 14 (App Router)、nestjs、TypeScript的智能对话平台，支持多模型、联网搜索、文件上传的本地知识库搭建。

### 前端

    技术栈：
        1. 核心框架：Next.js 14 (App Router)、TypeScript
        2. 样式与组件库：
            TailwindCSS：原子化css样式
            Shadcn UI：组件库，提供基础组件和交互效果
        3. 状态管理：
            Zustand：负责全局 UI 状态流转
            Vercel AI SDK (@ai-sdk/react)：专用于管理 AI 会话状态
            Framer Motion：处理右侧 Artifacts 面板平滑滑入、对话分支切换时的丝滑过渡动画
        4. 渲染引擎：
            AI 输出的流式文本安全地解析为 DOM 节点：react-markdown + remark-gfm + rehype-raw
            代码块语法高亮，支持所有主流语言，样式与主题高度契合：Shiki (或 Prism.js)
            @sandpack/react：用于渲染代码沙箱，展示 AI 生成的代码示例
    页面功能划分：
        模块一：全局布局与导航 (Layout & Navigation)
            - 响应式三栏框架：左侧导航、中栏对话、右侧沙箱
            - 主题系统：深浅色模式自适应切换 (Dark/Light Mode)
            - 历史会话管理：
                1. 无限滚动加载 (Infinite Scroll) 或分页加载历史记录。
                2. 会话按时间智能分组（Today, Previous 7 Days 等）。
                3. 会话标题的自动生成、手动重命名与删除
        模块二：核心对话交互 (Core Chat Interface)
                - 超级输入框 (Smart Textarea)：
                    1. 支持多行自动撑开高度（最大高度限制）
                    2. 支持 Cmd/Ctrl + Enter 快捷发送
                    3. 文件拖拽上传区域（Dropzone），支持图片预览
                    4. 集成“🌐 联网搜索” Toggle 开关
                    5.上场文件（RAG操作）
                - 流式对话流 (Streaming Feed)：
                    1. 打字机效果平滑渲染，解决 Markdown 解析过程中的闪烁问题。
                    2. 对话列表自动滚动到底部（Auto-scroll），且用户上滑浏览时自动暂停滚动
                - 消息气泡操作栏：
                    一键复制内容、一键复制单段代码、重新生成 (Regenerate)、删除单条消息
        模块三：高阶 AI 状态感知 (AI State Awareness)
                - 思考过程可视化 (Thinking UI)：
                    当拦截到 Tool Calling（例如正在联网搜集资料）时，渲染折叠面板展示进度（例如“🔍 正在检索 5 个网页...”）
                - 沉浸式引用溯源 (Citations)：
                    1. 将 AI 输出内容中的 [1], [2] 渲染为可点击的 Badge。
                    2. 鼠标悬浮 (Hover) 展示网页来源的 Tooltip 摘要。
        模块四：分支对话控制 (Conversation Branching)
                - 分支分页器：在单条消息底部渲染 < 1 / 3 > 的切换组件
                - 分支上下文切换：点击切换分支时，Zustand 更新 activeMessageId，自动重绘下方所有的子节点对话，实现无缝穿梭
                - 原位编辑 (Edit & Submit)：允许用户点击自己发出的历史消息进行修改，修改后自动生成一个新的分支链
        模块五：Artifacts 右侧工作区 (Workspace Sandbox)
                - 代码预览沙箱 (Code Preview)：
                    1. 解析中栏 AI 输出的 <thinkspace-code> 标签内容。
                    2. 右侧面板展开，使用 Sandpack 实时编译并渲染 React/HTML/JS 代码。
                    3. 提供代码刷新、新窗口打开功能
                - 沉浸式阅读器 (Web Reader)：
                    1. 点击中栏的文献引用时，右侧面板展开
                    2. 渲染目标网页的干净排版（Reader View），并高亮 AI 提取的核心段落

### 后端

    思路：查库拼数据 -> 调大模型 -> 处理工具 -> 流式推前端
    技术栈：
        1. 核心框架与通信：
            - NestJS
            - Server-Sent Events (SSE)：摒弃 WebSocket，使用 HTTP 长连接进行单向流式传输。在 NestJS 中可以通过 @Sse() 装饰器或直接操作 @Res() res: Response 来实现
        2. 数据库与持久化层 (Data Layer)
            - PostgreSQL： 为唯一的主数据库。关系型数据（用户、会话、消息树）和向量数据都在这里解决
            - Prisma ORM：schema.prisma 建模体验极佳，类型自动推导对前端转全栈非常友好
            - pgvector (Postgres 插件)：用于存储和检索高维向量（Embeddings），这是实现 RAG（检索增强生成）的最轻量级方案，无需额外部署 Pinecone 等独立向量库
        3. AI 与大模型编排 (AI Orchestration)
            - Vercel AI SDK (Server-side) 或 LangChain.js：在后端统一抹平 OpenAI、Claude、DeepSeek 等大模型 API 的差异
            - Tiktoken：用于在把上下文发给大模型前，精准计算 Token 数量，防止超出模型上下文窗口
        4. 外部工具；
            - Tavily API 或 Firecrawl：专为 LLM 设计的搜索引擎 API，直接返回干净的 Markdown 网页正文，用于实现“联网搜索”工具
            - AWS S3 / Cloudflare R2： 象存储，用于存放用户上传的 PDF、图片以及 AI 生成的静态资源

    功能划分：
        模块一：AuthModule (鉴权与用户模块)
            - JWT 签发与校验：实现登录注册逻辑，签发 Access Token
            - 全局 AuthGuard：拦截所有非法请求，解析 Token 并将 userId 注入到 Request 对象中
            - 用户额度管理：记录用户的 Token 消耗量，提供简单的防刷和限流（Rate Limiting）机制
        模块二：ChatModule (核心会话与树状消息管理)
            这是数据库操作最复杂的模块，需要配合 Prisma 实现分支逻辑
            - 会话 (Session) CRUD：新建对话、获取历史列表、自动或手动更新对话标题、逻辑删除
            - 树状消息 (Message Tree) 写入：接收到前端的消息时，不仅要存内容，还要准确绑定 parentId（父消息 ID），从而在数据库中形成一棵“对话树”
            - 上下文路径解析 (Context Resolution)：核心难点。当用户从前端传入一个 leafMessageId（当前分支的最后一条消息）时，后端需要通过递归查询（或 Postgres 的 CTE 递归 SQL），向上追溯到根节点，提取出这一整条分支的完整对话上下文
        模块三：LLMModule (大模型流式网关)
            这个模块不碰数据库，只负责和外部 AI 厂商打交道。
            - 统一生成接口：封装 generateStream 方法，接收组装好的上下文字符串，返回一个 Async Iterable（异步可迭代对象）或 ReadableStream
            - 滑动窗口与记忆压缩：当解析出的上下文 Token 超过阈值（如 8k）时，自动触发摘要机制，保留最近的 5 轮对话，将更早的对话压缩成一句 Summary，以节省 API 费用
            - SSE 事件流组装：将 LLM 返回的 Chunk（文本碎片）按照固定的格式（如 data: {"text": "..."}\n\n）推入 Response 流中
        模块四：AgentModule (工具调用与联网搜索)
            负责让大模型长出“手和眼”。
            - Tool Schema 注册：在向 LLM 发起请求时，携带定义好的工具描述（例如 search_web，参数为 query）。
            - 执行回调拦截：当 LLM 流式输出中断，并抛出“我要调用工具”的指令时，该模块接管控制权。
            - 状态透传：在服务端实际调用 Tavily API 进行搜索的过程中，向前端推送特殊的 SSE 事件（如 data: {"type": "tool_call", "status": "searching", "tool": "Tavily"}），驱动前端显示“思考过程 UI”。
        模块五：KnowledgeBaseModule (RAG 知识库与文档解析)
            处理用户上传的文件，实现外挂大脑。
            - 文件上传与文本提取：接收前端传来的 PDF/Word/TXT，存储到 S3，并提取出纯文本内容。
            - 文档切块 (Chunking)：将超长文本按照语义或固定字数（如 500 token/块）切割，并保留一定的上下文重叠（Overlap）。
            - 向量化 (Embedding)：调用 OpenAI 的 text-embedding-3-small 接口，将文本块转化为浮点数向量。
            - 向量检索 (Vector Search)：在用户提问时，先将问题向量化，然后去 Postgres (pgvector) 中进行余弦相似度查询，找出最相关的 Top 5 文本块，将其塞入 System Prompt 中发给大模型。

    接口设计概览 (API Endpoints)
        POST /auth/login - 登录并获取 JWT
        GET /sessions - 获取用户的历史会话列表
        GET /sessions/:id/messages - 获取特定会话的整棵消息树（前端用来渲染分支关系）
        POST /knowledge/upload - 上传文档并触发异步向量化任务
        POST /chat/stream - （绝对核心）接收用户提问 {"sessionId", "parentId", "content"}, 建立长连接并返回 SSE 数据流。
